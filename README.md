# CODFLO Monorepo

This repository contains:

- Flutter client in `client/`
- Go Fiber backend in `server/`
- FastAPI AI service in `agent/`
- Shared contracts in `shared/`
- Infra artifacts in `infra/`
- Automation scripts in `scripts/`
